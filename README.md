Simple PHP authentication and posting, MVC approach in pure OOP PHP not to get rusty because of too much Laravel :)

Create a database like suggested in db.sql file.

Set default URL and DB in config.php.